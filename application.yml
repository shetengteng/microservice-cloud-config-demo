spring:
  profiles:
    active:
     - dev

---
spring:
  profiles: dev
  application:
    name: microservicecloud-config-stt-dev

---
spring:
  profiles: test
  application:
    name: microservicecloud-config-stt-test